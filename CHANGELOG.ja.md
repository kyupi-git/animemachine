[中文](CHANGELOG.md) | [English](CHANGELOG.en.md) | [日本語](CHANGELOG.ja.md)
[README](README.ja.md) | [導入・利用ガイド](docs/guide.ja.md) | [アーキテクチャとデータベース](docs/architecture.ja.md) | [更新履歴](CHANGELOG.ja.md)

# 更新履歴

このファイルはユーザーに影響する変更を新しいバージョンから順に記録します。内部リファクタリング、テストのみの調整、利用に影響しない細部は個別に記載しません。

## 0.2.1

0.2.1 は Ani-RSS 連携、多言語タイトル、画像復旧の配布品質を重点的に改善します。0.2.0 の設定と Catalog との互換性を維持し、Catalog の再構築やメディアディレクトリの移行は不要です。

### 追加・改善

- 多言語タイトルを改善：カード、フィルター、詳細、関係図を UI 言語へ統一し、英語名がない場合は原題を表示するだけで英語名として保存しません。
- Ani-RSS 統合同期を改善：購読、再生メディア、リソース、カバーを独立して処理し、不完全な同期は最長 5 分の短周期で補償再試行します。API 障害時は古いリモートソースを停止し、ローカルライブラリ、Torrent、独立マウントした読み取り専用メディアを継続します。
- Ani-RSS のネットワーク復旧を改善：同期を実効ルート世代に結び付け、プロキシや `NO_PROXY` 変更後は旧スナップショットを外して再検証します。
- Ani-RSS の画像連携を改善：カバー内容を検証し、端点障害時は自前画像へ即時退避して、復旧後は古い `no_cover` 状態を解除します。
- Ani-RSS のリソース探索を改善：直近作品から24か月を遡り、失敗時は短周期で再試行し、前回未完了なら次回走査をスキップします。
- Ani-RSS のリソース利用モードを明確化：手動モードが停止するのはバックグラウンドのリソース探索と計画だけで、接続済み購読と API 再生可能メディアは同期間隔どおり更新し、復旧後も自動的に継続します。
- ゼロ設定の初回導入を改善：ローカル起動と4種類の Compose は既定で `0.0.0.0:8787` を待受し、LAN から到達可能な環境では認証を自動有効化して、自動生成した初期管理者資格情報を永続化し後続起動でも再表示します。`.env` は既定値の上書き時だけ必要で、`04-full-stack/compose.yaml` は Collector を内包して空ディレクトリへ単独コピーしても利用できます。管理対象 qBittorrent/Ani-RSS の秘密値は一度自動生成して再利用し、空のメディアマウントはサービス降格前に書き込み可能な状態へ準備します。完全構成では Ani-RSS の最終ダウンロード、qBittorrent から見えるパス、AnimeMachine の再生を共有 `/Media` に統一し、未完了データだけを `/downloads/incomplete` に分離します。

### 修正

- Ani-RSS の不正・途中応答を空結果として扱う問題を修正し、異常な購読・再生・リソース応答では直前の正常スナップショットを保持します。
- Ani-RSS の接続先、API Key、ルート変更後に旧検索結果が書き戻る問題を修正し、世代変更時は旧キャッシュを無効化して再探索します。
- localhost/LAN の Ani-RSS カバー障害クールダウンが無関係なプロキシ切替で解除される問題を修正し、ローカル直結のクールダウンをプロキシ変動から分離しました。リモート端点は実際のルート変更後に従来どおり再検証します。
- 時計の巻き戻しや異常な未来時刻により、画像負キャッシュ復旧、リソース再確認、外部読み取り専用メディア走査が遅れる問題を修正しました。
- Ani-RSS のオンデマンド接続失敗がローカル候補計画を中断する問題を修正し、任意サービス障害時も AnimeMachine 自身の候補を利用できます。
- ローカル起動が正式な 8787 ではなく回環/8877 を使用していた問題、互換 `serve/demo` 入口に回環待受/旧 8765 の既定値が残っていた問題、および `.env`、ディレクトリ上書き、管理サービスの事前入力秘密値がないと Compose を直接起動できない問題を修正しました。管理 Compose は資格情報 bootstrap の完了も待つようになり、空設定ディレクトリの初回起動競合を解消し、Docker のゼロ設定 Ani-RSS 状態同期間隔もアプリ／文書と同じ既定 30 分へ統一し、Windows 起動スクリプトの既存インスタンス確認も実際の待受アドレス（IPv6 を含む）へ追従するよう修正しました。
- 字幕検索の旧 Catalog フィールド参照による SQL エラーを修正し、設定と About から未実装 MyAnimeList 退避表記を削除しました。

## 0.2.0

0.2.0 は Ani-RSS の実行時統合を完成させ、ネットワーク、バックグラウンド処理、アプリ保守を強化しました。Torrent、ローカルメディア、従来の画像ソースは独立したフォールバック経路を維持し、旧版からの更新でも Catalog の再構築やメディアディレクトリの移行は不要です。

### 追加・改善

- Ani-RSS 統合同期を追加：購読、再生可能メディア、話数、カバー、リソース状態をまとめて更新し、一部失敗時は直前の正常スナップショットを保持します。
- Ani-RSS リモート再生中継を追加：メディアディレクトリをマウントしなくても完全な M3U を生成し、Range、シーク、短時間切断からの再開に対応します。
- アプリのオンライン更新を追加：ポータブル版は検証、ヘルスチェック、失敗時ロールバックに対応し、Docker は Docker Socket なしのアプリ層更新に対応します。
- システム健全性とネットワーク診断を追加：ネットワーク、ストレージ、画像、再生、qBittorrent、Ani-RSS を一括表示し、経路ごとの健全性を記録します。
- 中国、日本、韓国、米国、欧州、その他の地域を対象にしたリソース地域フィルターを追加しました。
- 画像読み込みとリソース事前準備を改善し、閲覧優先度、前景活動、システム負荷、ネットワーク状態に応じてバックグラウンド負荷を調整します。
- Torrent、Ani-RSS、ローカル、外部読み取り専用メディアのソースと所蔵状態を統一し、絞り込み、表示、再生、健全性判定を揃えました。
- 資格情報管理を改善：Web 保存のサービス資格情報を権限制限付き領域に置き、設定と資格情報をトランザクション保存し、デプロイ Secret を最優先にします。
- 日本以外のアニメのメタデータを改善し、実際の原語を保持しながら、多言語別名、原作関係、制作会社集約を最適化しました。

### 修正

- Ani-RSS の不完全応答、個別メディア更新失敗、時計巻き戻しにより、購読状態や再生可能話数が停滞または誤って消える問題を修正しました。
- Ani-RSS 切断、資格情報変更、任意メディアディレクトリ障害時に、古いリソースが計画されたり再生が上流待ちになったりする問題を修正しました。
- 地域許可、ソースフィルター、所蔵状態がカード、詳細、異なる DB 呼び出し経路で不一致になる問題を修正しました。
- 設定検証失敗時に設定や資格情報が部分保存される問題、および接続テストが新しい資格情報を先に保存・消去する問題を修正しました。
- 年跨ぎの冬期、未来日付、多言語タイトル、異常な年月状態が絞り込みや復旧時に誤解析される問題を修正しました。
- ファイルサイズ基準の再生キュー整理で別話を削除する問題と、重複 info-hash、パス大小文字、無効な Torrent コピーによる識別問題を修正しました。
- Compose の待受アドレス、オンライン更新のヘルスプローブ、プロキシ設定により、コンテナ到達不能や誤ロールバックが起きる問題を修正しました。
- 大容量ダウンロードのソース切替、破損一時ファイル再利用、画像オフライン計数、Linux/macOS 更新パッケージ種別検証を修正しました。
