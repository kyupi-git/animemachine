[中文](CHANGELOG.md) | [English](CHANGELOG.en.md) | [日本語](CHANGELOG.ja.md)
[README](README.md) | [部署与使用指南](docs/guide.md) | [架构与数据库](docs/architecture.md) | [更新日志](CHANGELOG.md)

# 更新日志

本文件按版本倒序记录面向用户的重要变化。内部重构、测试调整及不影响使用的细节不单独列出。

## 0.3.0

0.3.0 新增“新作雷达”，改进连载进度、作品详情和内存占用；兼容既有 Catalog、媒体目录及 0.2.x 配置，所需字段自动迁移，无需重建 Catalog 或搬迁媒体。

### 新增与改进

- 新增“新作雷达”：提供上/本/下季度分页视图，季度切换前七天自动滚动；展示标题、媒介、最新/总集数、订阅状态和更新时间，支持三语标题、未读高亮及直接订阅。
- 改进新作发现：电影可按上映、网络上线或 BD/Blu-ray 发行日期进入相应季度；连载按真实新集证据排序，未订阅作品也可通过资源扫描发现更新。
- 改进作品集数：详情与雷达统一显示本作进度，在证据明确时将系列累计编号换算为当前季度/篇章编号，并优先采用本作已知总集数。
- 改进详情页：收藏库与播放来源分别展示，减少重复媒体信息；字幕检索使用统一入口，尚无媒体或已暂停的 Ani-RSS 订阅也可管理。
- 改进默认设置：“可用来源”初始和重置时均为三项全选，兼容旧版 `available` 默认值；M3U 播放保持默认启用，移除多余总开关。
- 降低首次建库及周期扫描内存：精简 Archive 中间数据，按需构建标题索引，限制解析缓存，流式处理 Torrent 行，并保证异常退出释放数据库连接。

### 修复

- 修复重下旧集、集数回退后恢复或下载时间回拨可能造成的新集误报；断连时停用旧追更证据，恢复连接后重新启用。
- 修复共享物理目录的作品相互继承追更状态，以及订阅删除或重新映射后残留旧集数、下载时间和媒体快照的问题。
- 修复创建用户的必填项阻止保存其它设置、保存失败提前改变运行配置，以及保存时丢失额外媒体库配置的问题。
- 修复快速切换详情或播放来源时旧响应覆盖新选择，以及雷达已读记录达到上限后近期阅读记录被错误淘汰的问题。
- 修复标题搜索可能绕过雷达筛选或地区许可、畸形发行日期被接受的问题；同时加强配置备份的发布排除检查。

## 0.2.1

0.2.1 重点提升 Ani-RSS 协同、三语标题与图片恢复的交付稳定性；保持 0.2.0 配置和 Catalog 兼容，升级无需重建 Catalog 或迁移媒体目录。

### 新增与改进

- 改进三语标题链路：卡片、筛选、详情和关系图统一跟随界面语言；可靠英文名缺失时仅显示作品原标题，不把回退值误存为英文。
- 改进 Ani-RSS 统一同步：订阅、媒体、资源和封面独立容错；同步不完整时在不超过 5 分钟的短周期内补偿重试；API 不可用时停用旧远程来源，本地收藏、Torrent 与独立挂载的只读媒体继续运行。
- 改进 Ani-RSS 网络恢复：同步绑定实际代理路由代次；代理或 `NO_PROXY` 切换后旧快照退出可用状态并立即补偿验证。
- 改进 Ani-RSS 图片协同：远程封面先校验内容；端点故障快速回退自有图片链路，恢复后解除旧 `no_cover` 缓存。
- 改进 Ani-RSS 资源发现：最近作品优先并按滚动 24 个月补齐；失败采用短周期重试，上一轮未结束时跳过新扫描而不并发排队。
- 明确 Ani-RSS 资源调用模式：手动模式只停止后台资源发现与规划，已连接的订阅和 API 可播放媒体仍按同步间隔更新，恢复连接后无需重新启用。
- 改进零配置首次部署：本地启动与四套 Compose 默认监听 `0.0.0.0:8787`；远程可访问时自动启用认证，自动生成的初始管理员凭据会持久保存并在后续启动继续显示；`.env` 仅用于覆盖默认值；`04-full-stack/compose.yaml` 自包含 Collector，可单独复制到空目录部署；托管 qBittorrent/Ani-RSS 所需密钥可自动生成并持久复用，空媒体挂载点会在服务降权前准备写权限；完整栈把 Ani-RSS 最终下载、qBittorrent 可见路径与 AnimeMachine 播放统一到共享 `/Media`，未完成数据单独使用 `/downloads/incomplete`。

### 修复

- 修复 Ani-RSS 畸形或截断响应被误当成完整空结果的问题；异常订阅、播放列表和资源响应会失败关闭并保留最近有效快照。
- 修复 Ani-RSS 地址、API Key 或代理路由切换后旧资源晚到写回的问题；来源代次变化会失效旧缓存并重新进入资源扫描。
- 修复 localhost/LAN Ani-RSS 的封面端点故障冷却会被无关代理切换提前解除的问题；本地直连冷却不再受代理抖动影响，远程端点仍会在实际路由变化后重新验证。
- 修复系统时钟回拨或异常未来时间戳可能长期压住图片负缓存、资源复查和外部只读媒体扫描的问题。
- 修复 Ani-RSS 按需资源查询连接失败中断本地候选规划的问题；可选服务异常时继续使用 AnimeMachine 自身候选。
- 修复本地启动器仍使用回环地址/8877 与正式 8787 端口不一致、底层兼容 `serve/demo` 入口仍保留回环监听/旧 8765 默认端口，以及 Compose 缺少 `.env`、目录变量或预填服务密钥时无法直接启动的问题；托管 Compose 现在还会等待密钥 bootstrap 完成，消除首次空配置目录下的启动竞态，并把 Docker 零配置 Ani-RSS 状态同步默认值与程序/文档统一为 30 分钟；Windows 启动器的在运行实例探测也会跟随实际监听地址，并正确处理 IPv6。
- 修复字幕检索旧 Catalog 字段可能触发 SQL 错误的问题，并移除配置与关于页中未启用的 MyAnimeList 回退声明。

## 0.2.0

0.2.0 重点完成 Ani-RSS 运行期整合，并升级网络、后台任务与应用维护能力。Torrent、本地媒体和原有图片源继续作为独立回退路径；从旧版升级无需重建 Catalog 或迁移媒体目录。

### 新增与改进

- 新增 Ani-RSS 统一同步：集中刷新订阅、可播放媒体、集数、封面与资源状态；局部失败时保留最近有效快照并自动补偿。
- 新增 Ani-RSS 远程播放中继：未挂载其媒体目录时仍可生成完整 M3U，并支持 Range、拖动及短暂断线续传。
- 新增应用在线升级：便携版支持校验、健康检查与失败回滚；Docker 支持无需挂载 Docker Socket 的应用层更新。
- 新增系统健康与网络诊断：集中展示网络、存储、图片、播放、qBittorrent 与 Ani-RSS 状态，并记录不同网络路径的健康度。
- 新增资源地区筛选：支持按中国、日本、韩国、美国、欧洲及其它地区控制作品和资源范围。
- 改进图片加载与资源预热：按浏览优先级分批调度，并依据前台活动、系统负载和网络状态动态控制后台开销。
- 改进来源与收藏状态：统一 Torrent、Ani-RSS、本地和外部只读媒体的筛选、展示、播放及健康状态判断。
- 改进凭据管理：Web 保存的服务凭据进入权限受限的独立存储，配置与凭据采用事务提交，部署 Secret 仍具有最高优先级。
- 改进非日本动画元数据：保留实际原始语言，并优化多语言别名、原作关系和制作公司聚合。

### 修复

- 修复 Ani-RSS 不完整响应、单项媒体刷新失败或系统时钟回拨时，订阅状态和可播放集数可能停滞或被误清除的问题。
- 修复 Ani-RSS 断连、凭据变更及可选媒体目录不可用时，旧资源仍被规划、播放请求等待上游或后台线程异常的问题。
- 修复地区许可、来源筛选和收藏状态在卡片、详情及不同数据库调用路径中结果不一致的问题。
- 修复设置校验失败时配置或凭据可能部分写入，以及“测试连接”提前保存或清空新凭据的问题。
- 修复跨年冬季、未来日期、多语言标题及异常年月状态在筛选或恢复时解析不准确的问题。
- 修复播放队列按文件大小误删不同集数，以及重复 info-hash、路径大小写和 Torrent 副本失效导致的资源识别问题。
- 修复 Compose 监听地址、在线升级健康探测及代理配置异常导致的容器不可达或错误回滚问题。
- 修复大文件下载切源、损坏临时文件复用、图片离线计数及 Linux/macOS 更新包类型校验问题。
